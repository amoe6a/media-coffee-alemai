services:
  bot:
    build: .
    container_name: coffee-bot
    env_file: .env
    environment:
      - DB_PATH=/app/data/coffee.db
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
